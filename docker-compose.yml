version: '3.8'

services:
  web:
    build: .
    container_name: tooth-strength-fog-node
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    env_file:
      - .env
    restart: unless-stopped
